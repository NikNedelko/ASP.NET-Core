@model Product[]
@{
    ViewBag.Title = "Product Name";
}

<table>
    <thead>
        <trXth>Name</thXth>Price</th>
            </tr>
    </thead>
    <tbody>
        @foreach (Product p in Model)
        { 
            <tr>
                <td>@p.Name</td>
                <td>@($"{p.Price:C2}")</td>
            </tr>
        }
         </tbody>
</table>
